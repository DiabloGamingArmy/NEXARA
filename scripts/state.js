/**
 * MODULE: State Management (State Layer)
 * -------------------------------------------------------------------------
 * ACTS AS: Centralized Mutable State Container & Observer Pattern Registry
 * RESPONSIBILITY: Maintains runtime memory (currentUser, caches) and
 * manages the ListenerRegistry to prevent memory leaks from open sockets.
 * -------------------------------------------------------------------------
 */
